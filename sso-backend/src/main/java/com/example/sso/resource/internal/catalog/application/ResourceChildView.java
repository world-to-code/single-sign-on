package com.example.sso.resource.internal.catalog.application;

/** A direct child resource of a {@link ResourceView} (id + display name). */
public record ResourceChildView(String id, String name) {
}

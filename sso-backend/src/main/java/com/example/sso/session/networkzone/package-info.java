/**
 * Named interface for network-zone administration: the {@link NetworkZoneService} facade, its spec/view DTOs
 * and the IP-rule value types that session policies reference. Zone and CIDR persistence stays module-internal.
 */
@NamedInterface("networkzone")
package com.example.sso.session.networkzone;

import org.springframework.modulith.NamedInterface;

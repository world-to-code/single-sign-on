package com.example.sso.mapping;

/** What a mapping rule assigns matching users to: a group's membership, a role, or a resource's membership. */
public enum MappingTargetKind {
    GROUP,
    ROLE,
    RESOURCE_MEMBER
}
